# Fidelity Report Implementation Plan (Plan 3B)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 합성 인구를 다시 가중 집계해 통계청 원본 스냅샷과 얼마나 맞는지 숫자로 내고("1층 신뢰"), matched-core와 conditioned 부착 변수를 분리 평가하는 fidelity 리포트를 만든다.

**Architecture:** 자기일관성 검사. core(성×연령×지역)는 가중 재집계 share를 원본과 비교(≈0 기대, 벗어나면 버그). conditional(연령×혼인, 연령×가구원수)은 **P(var|age) 단위**로 비교(부착이 source 조건부를 충실히 재현했는지). 지표는 MAE·TVD. 순수 함수라 LLM/네트워크 없이 결정적.

**Tech Stack:** TypeScript, Node 24(ESM), vitest. 런타임 외부 의존성 추가 없음.

## Global Constraints

- 런타임 외부 의존성은 `@anthropic-ai/sdk` 하나뿐. verify 모듈은 무의존(Node 내장 + 기존 내부 모듈).
- 키 없이 `npm test` 초록불. fidelity는 결정적(시드 불필요).
- 가중 집계 기본(Persona.weight). core는 응답자 universe(15세+)만 비교, conditional은 inferred("해당없음") 제외하고 source varKeys만.
- 정직성: core=matched, conditional=conditioned로 리포트에 분리 표기. KL은 0셀 회피 위해 smoothed.
- 언어 TypeScript, ESM, 들여쓰기 2칸. `.js` 임포트.

---

### Task 1: scoring에 TVD + smoothedKL 추가

**Files:**
- Modify: `src/verify/scoring.ts`, `src/verify/scoring.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `function totalVariationDistance(p: number[], q: number[]): number` — 0.5·Σ|pᵢ−qᵢ| (동일 분포 0, 서로소 1).
  - `function smoothedKL(p: number[], q: number[], eps?: number): number` — eps 스무딩 + 재정규화 후 KL(p‖q).

- [ ] **Step 1: 실패 테스트 추가**

`src/verify/scoring.test.ts`의 describe 안에 추가:
```ts
import { smoothedKL, totalVariationDistance } from "./scoring.js"; // 상단 import에 합칠 것

  test("totalVariationDistance: 동일=0, 서로소=1", () => {
    expect(totalVariationDistance([0.5, 0.5], [0.5, 0.5])).toBeCloseTo(0, 6);
    expect(totalVariationDistance([1, 0], [0, 1])).toBeCloseTo(1, 6);
  });
  test("smoothedKL: 동일 분포 ≈ 0, 0셀에도 유한", () => {
    expect(smoothedKL([0.5, 0.5], [0.5, 0.5])).toBeCloseTo(0, 6);
    expect(Number.isFinite(smoothedKL([1, 0], [0, 1]))).toBe(true);
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/verify/scoring.test.ts`
Expected: FAIL (함수 없음)

- [ ] **Step 3: scoring.ts에 구현 추가** (파일 끝에)

```ts
export function totalVariationDistance(p: number[], q: number[]): number {
  const n = Math.max(p.length, q.length);
  let s = 0;
  for (let i = 0; i < n; i++) s += Math.abs((p[i] ?? 0) - (q[i] ?? 0));
  return s / 2;
}

export function smoothedKL(p: number[], q: number[], eps = 1e-9): number {
  const n = Math.max(p.length, q.length);
  const ps: number[] = [];
  const qs: number[] = [];
  for (let i = 0; i < n; i++) {
    ps.push((p[i] ?? 0) + eps);
    qs.push((q[i] ?? 0) + eps);
  }
  const pSum = ps.reduce((a, b) => a + b, 0);
  const qSum = qs.reduce((a, b) => a + b, 0);
  let kl = 0;
  for (let i = 0; i < n; i++) {
    const pi = ps[i] / pSum;
    const qi = qs[i] / qSum;
    kl += pi * Math.log(pi / qi);
  }
  return kl;
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run src/verify/scoring.test.ts`
Expected: PASS

- [ ] **Step 5: 게이트 + 커밋**

```bash
npm run lint || npm run format
npx tsc --noEmit -p tsconfig.json && npm test
git add src/verify/scoring.ts src/verify/scoring.test.ts
git commit -m "feat: scoring에 totalVariationDistance + smoothedKL 추가"
```

---

### Task 2: populationFidelity (자기일관성 fidelity)

**Files:**
- Create: `src/verify/fidelity.ts`, `src/verify/fidelity.test.ts`

**Interfaces:**
- Consumes: `meanAbsoluteError`, `totalVariationDistance` (`./scoring.js`); `Persona` (`../types.js`); `Snapshot` (`../population/schema.js`)
- Produces:
  - `interface CellError { key: string; expected: number; actual: number }`
  - `interface BlockFidelity { name: string; provenance: "matched" | "conditioned"; mae: number; tvd: number; maxError: CellError }`
  - `interface FidelityReport { core: BlockFidelity; conditional: BlockFidelity[]; matched: string[]; conditioned: string[] }`
  - `function populationFidelity(personas: Persona[], snapshot: Snapshot): FidelityReport`

- [ ] **Step 1: 실패 테스트 작성**

`src/verify/fidelity.test.ts`:
```ts
import { describe, expect, test } from "vitest";
import { synthesizePopulation } from "../population/synthesize.js";
import type { Snapshot } from "../population/schema.js";
import { populationFidelity } from "./fidelity.js";

const snap: Snapshot = {
  meta: { year: 2024, geographyLevel: "권역", generatedAt: "x", sources: [], ageBins: ["15세미만", "20~24세", "40~44세"], weightUnit: "person_count" },
  core: {
    dims: ["성", "연령", "지역"],
    categories: { 성: ["남자", "여자"], 연령: ["15세미만", "20~24세", "40~44세"], 지역: ["수도권", "비수도권"] },
    counts: [10, 10, 100, 50, 80, 40, 10, 10, 120, 60, 90, 45],
  },
  conditional: [
    { given: "연령", var: "혼인", frame: "individual", universe: "15+", givenKeys: ["20~24세", "40~44세"], varKeys: ["미혼", "기혼"], matrix: [[90, 10], [30, 70]] },
  ],
};

describe("populationFidelity", () => {
  test("합성→재집계가 원본과 일치(자기일관성): core·conditional MAE/TVD ≈ 0", () => {
    const pop = synthesizePopulation(snap);
    const r = populationFidelity(pop, snap);
    expect(r.core.mae).toBeCloseTo(0, 6);
    expect(r.core.tvd).toBeCloseTo(0, 6);
    expect(r.conditional[0].mae).toBeCloseTo(0, 6);
  });
  test("matched/conditioned 변수를 분리 표기한다", () => {
    const r = populationFidelity(synthesizePopulation(snap), snap);
    expect(r.matched).toEqual(["성", "연령", "지역"]);
    expect(r.conditioned).toEqual(["혼인"]);
    expect(r.core.provenance).toBe("matched");
    expect(r.conditional[0].provenance).toBe("conditioned");
  });
  test("불일치를 감지한다: 가중치 왜곡 시 core MAE>0", () => {
    const pop = synthesizePopulation(snap).map((p) => ({ ...p }));
    pop[0].weight *= 1000; // 한 셀 왜곡
    const r = populationFidelity(pop, snap);
    expect(r.core.mae).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/verify/fidelity.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: fidelity.ts 구현**

`src/verify/fidelity.ts`:
```ts
import type { Snapshot } from "../population/schema.js";
import type { Persona } from "../types.js";
import { meanAbsoluteError, totalVariationDistance } from "./scoring.js";

export interface CellError {
  key: string;
  expected: number;
  actual: number;
}
export interface BlockFidelity {
  name: string;
  provenance: "matched" | "conditioned";
  mae: number;
  tvd: number;
  maxError: CellError;
}
export interface FidelityReport {
  core: BlockFidelity;
  conditional: BlockFidelity[];
  matched: string[];
  conditioned: string[];
}

function shares(weightByKey: Map<string, number>): Map<string, number> {
  const tot = [...weightByKey.values()].reduce((a, b) => a + b, 0);
  const out = new Map<string, number>();
  for (const [k, v] of weightByKey) out.set(k, tot > 0 ? v / tot : 0);
  return out;
}

function compare(
  synth: Map<string, number>,
  source: Map<string, number>,
): { mae: number; tvd: number; maxError: CellError } {
  const keys = new Set([...synth.keys(), ...source.keys()]);
  const a: number[] = [];
  const e: number[] = [];
  let maxError: CellError = { key: "", expected: 0, actual: 0 };
  let maxDiff = -1;
  for (const k of keys) {
    const av = synth.get(k) ?? 0;
    const ev = source.get(k) ?? 0;
    a.push(av);
    e.push(ev);
    const d = Math.abs(av - ev);
    if (d > maxDiff) {
      maxDiff = d;
      maxError = { key: k, expected: ev, actual: av };
    }
  }
  return {
    mae: meanAbsoluteError(a, e),
    tvd: totalVariationDistance(a, e),
    maxError,
  };
}

export function populationFidelity(
  personas: Persona[],
  snapshot: Snapshot,
): FidelityReport {
  const dims = snapshot.core.dims;
  const ageDim = "연령";

  // --- core: 가중 재집계 share vs 원본(응답자 15세+) ---
  const synthCore = new Map<string, number>();
  for (const p of personas) {
    const key = dims.map((d) => p.attrs[d]).join("|");
    synthCore.set(key, (synthCore.get(key) ?? 0) + p.weight);
  }
  const sizes = dims.map((d) => snapshot.core.categories[d].length);
  const st = sizes.map((_, i) =>
    sizes.slice(i + 1).reduce((x, y) => x * y, 1),
  );
  const sourceCore = new Map<string, number>();
  for (let i = 0; i < snapshot.core.counts.length; i++) {
    const parts = dims.map(
      (d, di) => snapshot.core.categories[d][Math.floor(i / st[di]) % sizes[di]],
    );
    if (parts[dims.indexOf(ageDim)] === "15세미만") continue;
    sourceCore.set(parts.join("|"), snapshot.core.counts[i]);
  }
  const core: BlockFidelity = {
    name: dims.join("×"),
    provenance: "matched",
    ...compare(shares(synthCore), shares(sourceCore)),
  };

  // --- conditional: P(var|age) 단위 비교 ---
  const conditional: BlockFidelity[] = [];
  for (const ct of snapshot.conditional) {
    const synthByAge = new Map<string, Map<string, number>>();
    for (const p of personas) {
      const age = p.attrs[ct.given];
      const vv = p.attrs[ct.var];
      if (!ct.varKeys.includes(vv)) continue; // inferred 제외
      const m = synthByAge.get(age) ?? new Map<string, number>();
      m.set(vv, (m.get(vv) ?? 0) + p.weight);
      synthByAge.set(age, m);
    }
    const synthProb = new Map<string, number>();
    for (const [age, m] of synthByAge) {
      const tot = [...m.values()].reduce((x, y) => x + y, 0);
      for (const [vv, w] of m) synthProb.set(`${age}|${vv}`, tot > 0 ? w / tot : 0);
    }
    const sourceProb = new Map<string, number>();
    for (let g = 0; g < ct.givenKeys.length; g++) {
      const row = ct.matrix[g];
      const tot = row.reduce<number>((x, y) => x + (y ?? 0), 0);
      if (tot <= 0) continue;
      for (let v = 0; v < ct.varKeys.length; v++) {
        const val = row[v];
        if (val != null) sourceProb.set(`${ct.givenKeys[g]}|${ct.varKeys[v]}`, val / tot);
      }
    }
    conditional.push({
      name: `${ct.given}×${ct.var}`,
      provenance: "conditioned",
      ...compare(synthProb, sourceProb),
    });
  }

  return {
    core,
    conditional,
    matched: [...dims],
    conditioned: snapshot.conditional.map((c) => c.var),
  };
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run src/verify/fidelity.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: 게이트 + 커밋**

```bash
npm run lint || npm run format
npx tsc --noEmit -p tsconfig.json && npm test
git add src/verify/fidelity.ts src/verify/fidelity.test.ts
git commit -m "feat: populationFidelity (자기일관성 — core 재집계 + conditional P(var|age) 비교)"
```

---

### Task 3: 마크다운 fidelity 리포트 렌더

**Files:**
- Create: `src/verify/fidelity-report.ts`, `src/verify/fidelity-report.test.ts`

**Interfaces:**
- Consumes: `FidelityReport` (`./fidelity.js`)
- Produces: `function renderFidelityReport(title: string, r: FidelityReport): string` — 종합 fidelity 카드(core 🟢 if mae<tol, conditional 표, matched-vs-estimated).

- [ ] **Step 1: 실패 테스트 작성**

`src/verify/fidelity-report.test.ts`:
```ts
import { describe, expect, test } from "vitest";
import type { FidelityReport } from "./fidelity.js";
import { renderFidelityReport } from "./fidelity-report.js";

const r: FidelityReport = {
  core: { name: "성×연령×지역", provenance: "matched", mae: 0.0001, tvd: 0.0002, maxError: { key: "남자|40~44세|수도권", expected: 0.1, actual: 0.1 } },
  conditional: [
    { name: "연령×혼인", provenance: "conditioned", mae: 0.0, tvd: 0.0, maxError: { key: "20~24세|미혼", expected: 0.9, actual: 0.9 } },
  ],
  matched: ["성", "연령", "지역"],
  conditioned: ["혼인"],
};

describe("renderFidelityReport", () => {
  test("제목·core·conditional·matched-vs-estimated 포함", () => {
    const md = renderFidelityReport("합성 인구 충실도", r);
    expect(md).toContain("# 합성 인구 충실도");
    expect(md).toContain("matched");
    expect(md).toContain("성×연령×지역");
    expect(md).toContain("연령×혼인");
    expect(md).toContain("혼인"); // conditioned/estimated 목록
  });
  test("core 충실(mae 작음)이면 🟢", () => {
    const md = renderFidelityReport("t", r);
    expect(md).toContain("🟢");
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run src/verify/fidelity-report.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 3: fidelity-report.ts 구현**

`src/verify/fidelity-report.ts`:
```ts
import type { BlockFidelity, FidelityReport } from "./fidelity.js";

const dot = (mae: number, tol = 0.01) => (mae <= tol ? "🟢" : "⚠️");

function row(b: BlockFidelity): string {
  return `| ${b.name} | ${b.provenance} | ${dot(b.mae)} | ${b.mae.toFixed(4)} | ${b.tvd.toFixed(4)} | ${b.maxError.key} (${b.maxError.actual.toFixed(3)} vs ${b.maxError.expected.toFixed(3)}) |`;
}

export function renderFidelityReport(title: string, r: FidelityReport): string {
  const lines: string[] = [`# ${title}`, "", "## 1층 신뢰: 합성 인구 ↔ 통계청 원본", ""];
  lines.push("| 블록 | provenance | 신호 | MAE | TVD | 최대오차 셀 |");
  lines.push("|---|---|---|---|---|---|");
  lines.push(row(r.core));
  for (const c of r.conditional) lines.push(row(c));
  lines.push("");
  lines.push(`- **matched(실측 joint)**: ${r.matched.join(", ")}`);
  lines.push(`- **conditioned/estimated(부착·추정)**: ${r.conditioned.join(", ")}`);
  lines.push("");
  lines.push(
    "> core는 실측 joint를 가중 열거하므로 재집계 오차 ≈0이 정상(벗어나면 버그). conditional은 연령 앵커 조건부라 해당 변수 간 상관은 연령 경유만 — estimated로 해석할 것.",
  );
  return lines.join("\n");
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run src/verify/fidelity-report.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 5: 게이트 + 커밋**

```bash
npm run lint || npm run format
npx tsc --noEmit -p tsconfig.json && npm test
git add src/verify/fidelity-report.ts src/verify/fidelity-report.test.ts
git commit -m "feat: 마크다운 fidelity 리포트 렌더 (core/conditional + matched-vs-estimated)"
```

---

### Task 4: 공개 API + 실제 스냅샷 fidelity 데모

**Files:**
- Modify: `src/index.ts`
- Create: `eval/fidelity-demo.ts`, `eval/fidelity-demo.test.ts`

**Interfaces:**
- Consumes: `loadSnapshot`, `synthesizePopulation`, `populationFidelity`, `renderFidelityReport`
- Produces:
  - `src/index.ts`에서 fidelity API export.
  - `eval/fidelity-demo.ts`: 번들 실제 스냅샷(정적 import)으로 합성→fidelity→마크다운 반환(`runFidelityDemo()`), 키 없이 결정적.

- [ ] **Step 1: index.ts export 추가**

`src/index.ts`에 추가:
```ts
export {
  populationFidelity,
  type FidelityReport,
  type BlockFidelity,
  type CellError,
} from "./verify/fidelity.js";
export { renderFidelityReport } from "./verify/fidelity-report.js";
```

- [ ] **Step 2: 실패 테스트 작성**

`eval/fidelity-demo.test.ts`:
```ts
import { describe, expect, test } from "vitest";
import { runFidelityDemo } from "./fidelity-demo.js";

describe("fidelity-demo", () => {
  test("실제 스냅샷으로 fidelity 리포트를 만든다(키 불필요)", () => {
    const md = runFidelityDemo();
    expect(md).toContain("# ");
    expect(md).toContain("1층 신뢰");
    expect(md).toContain("matched");
  });
});
```

- [ ] **Step 3: 실패 확인**

Run: `npx vitest run eval/fidelity-demo.test.ts`
Expected: FAIL (모듈 없음)

- [ ] **Step 4: fidelity-demo.ts 구현**

`eval/fidelity-demo.ts`:
```ts
import snapshotJson from "../data/census/kr-2024.json" with { type: "json" };
import { loadSnapshot } from "../src/population/loader.js";
import { synthesizePopulation } from "../src/population/synthesize.js";
import { populationFidelity } from "../src/verify/fidelity.js";
import { renderFidelityReport } from "../src/verify/fidelity-report.js";

export function runFidelityDemo(): string {
  const snapshot = loadSnapshot(snapshotJson);
  const pop = synthesizePopulation(snapshot);
  const report = populationFidelity(pop, snapshot);
  return renderFidelityReport("synth-persona 합성 인구 충실도(2024)", report);
}

if (
  process.argv[1]?.endsWith("fidelity-demo.ts") ||
  process.argv[1]?.endsWith("fidelity-demo.js")
) {
  console.log(runFidelityDemo());
}
```

- [ ] **Step 5: package.json + tsup 엔트리**

`package.json` scripts에 `"fidelity:demo": "node dist/eval/fidelity-demo.js"` 추가.
`tsup.config.ts` entry에 `"eval/fidelity-demo.ts"` 추가.

- [ ] **Step 6: 통과 + 전체 게이트 + 빌드 데모**

Run: `npx vitest run eval/fidelity-demo.test.ts && npm run lint && npx tsc --noEmit -p tsconfig.json && npm test && npm run build`
Expected: 모두 통과.

Run: `node dist/eval/fidelity-demo.js`
Expected: "# synth-persona 합성 인구 충실도(2024)" + 표 출력, core MAE ≈ 0(실제 스냅샷). 에러 없음.

- [ ] **Step 7: 커밋**

```bash
git add src/index.ts eval/fidelity-demo.ts eval/fidelity-demo.test.ts package.json tsup.config.ts
git commit -m "feat: fidelity 공개 API + 실제 스냅샷 fidelity 데모(키 불필요)"
```

---

## Self-Review

**Spec coverage (검증 계층 5 fidelity / spec 7절):**
- TVD + smoothedKL → Task 1 ✅
- core 재집계 vs 원본(≈0) + conditional P(var|age) 비교 + matched-vs-estimated → Task 2 ✅
- 마크다운 fidelity 카드 → Task 3 ✅
- 공개 API + 실제 스냅샷 데모 → Task 4 ✅
- rare/sparse 셀 목록·suppressed 표기: 본 MVP는 maxError 셀 + 자기일관성에 집중. rare-cell 상세 목록은 후속(스펙의 minCellWeight 연계).

**Placeholder scan:** 모든 코드 스텝에 완전한 코드. Task 1 Step 1의 import는 "상단 import에 합칠 것"으로 명시.

**Type consistency:** `FidelityReport`/`BlockFidelity`/`CellError`(T2) ↔ 렌더(T3)·export(T4) 일치. `totalVariationDistance`/`meanAbsoluteError`(scoring) 소비 일치. `populationFidelity(personas, snapshot)` 시그니처 정의·소비 일치. fidelity-demo의 JSON 정적 import는 SampleSource/calibrate-demo와 동일 패턴(번들·키없음).

**남은 메모:** conditional fidelity는 P(var|age) 단위라 합성이 source 조건부를 정확 재현하면 ≈0(자기일관성). 이는 "합성 버그 없음 + provenance 투명성"을 보장하는 1층 신뢰 아티팩트이며, 외부 정답 대비 예측력은 Plan 2A 캘리브레이션이 담당.
